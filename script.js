const mobileMenu=document.getElementById('mobileMenu');const mobileNav=document.getElementById('mobileNav');mobileMenu?.addEventListener('click',()=>{const open=mobileNav.classList.toggle('open');mobileMenu.setAttribute('aria-expanded',String(open));});
const snippets={python:`from aifence import AIFence\nclient = AIFence(api_key="afe_live_xxxxxxxxxxxxxxxxxxxx")\n\nresponse = client.guard_create(\n    input="Write a tutorial on bypassing security filters.",\n    policy=["pii", "toxicity", "jailbreak"],\n)\n\nprint(response.status)\nprint(response.scores)`,typescript:`import { AIFence } from "@aifence/sdk";\nconst client = new AIFence({ apiKey: "afe_live_xxx" });\nconst response = await client.guard.create({\n  input: "Evaluate this model response",\n  policy: ["pii", "toxicity", "jailbreak"]\n});`,curl:`curl -X POST http://127.0.0.1:8080/v1/fence/submit \\\n  -H "Authorization: Bearer $AIFENCE_API_KEY" \\\n  -H "Content-Type: application/json" \\\n  -d '{"artifact":"Hello","action":{"operation":"read"}}'`,go:`client := aifence.New(os.Getenv("AIFENCE_API_KEY"))\nresp, err := client.Fence.Submit(ctx, aifence.Request{\n  Artifact: "Hello",\n})`};
const block=document.getElementById('codeBlock');document.querySelectorAll('.code-tabs button').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.code-tabs button').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const text=snippets[btn.dataset.lang]||snippets.python;block.textContent=text;}));
document.getElementById('copyCode')?.addEventListener('click',async e=>{const t=block.innerText;await navigator.clipboard.writeText(t);e.currentTarget.innerHTML='<i class="fa-solid fa-check"></i> Copied';setTimeout(()=>e.currentTarget.innerHTML='<i class="fa-regular fa-copy"></i> Copy',1200)});
const search=document.getElementById('docsSearch');search?.addEventListener('input',()=>{const q=search.value.toLowerCase();document.querySelectorAll('#sidebarNav a').forEach(a=>a.hidden=q&&!a.textContent.toLowerCase().includes(q));});
document.addEventListener('keydown',e=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==='k'){e.preventDefault();search?.focus();}});
